services:
  tests:
    build:
      context: .
      dockerfile: Dockerfile
    command: bun test

  verify:
    build:
      context: .
      dockerfile: Dockerfile
    command: sh -lc "bun run lint && bun run typecheck && bun test && bun run build"
